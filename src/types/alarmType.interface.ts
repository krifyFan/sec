export interface AlarmType {
    id ? : number,
        name ? : string,
        objectType ? : number,
        category ? : number,
        categoryName ?: string
        level ? : number,
        levelName ? : string
        description ? : string,

        objectTypeName ? : string,
}